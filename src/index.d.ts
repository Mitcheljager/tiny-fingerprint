/**
 * Returns a small fingerprint based on the user's browser, used for trend detection.
 * @param additionalData Optional array of extra data.
 * @returns An 8-character fingerprint hash.
 */
export function getTinyFingerprint(additionalData?: any[]): string;
